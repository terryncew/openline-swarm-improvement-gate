[project]
name = "openline-swarm-improvement-gate"
version = "0.1.3"
description = "A tiny mutation gate for self-improving agent swarms."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Terrynce White"}]

[project.scripts]
olp-swarm-gate = "olp_swarm_gate.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
